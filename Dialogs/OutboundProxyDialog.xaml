<Window x:Class="CrimsonOnion.Dialogs.OutboundProxyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OUTBOUND PROXY CONFIGURATION" Height="263" Width="420"
        WindowStartupLocation="CenterOwner" Background="Transparent" Foreground="#E2E8F0"
        ResizeMode="NoResize" FontFamily="Segoe UI" ShowInTaskbar="False"
        WindowStyle="None" AllowsTransparency="True">
    <Window.Resources>
        <SolidColorBrush x:Key="BgPanel"     Color="#121417"/>
        <SolidColorBrush x:Key="BgInput"     Color="#0A0C0F"/>
        <SolidColorBrush x:Key="BorderMain"  Color="#2D3748"/>
        <SolidColorBrush x:Key="BorderLight" Color="#3A3F44"/>
        <SolidColorBrush x:Key="TextMain"    Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextMuted"   Color="#A0AEC0"/>
        <SolidColorBrush x:Key="TextGreen"   Color="#68D391"/>
        <SolidColorBrush x:Key="ColorOk"     Color="#4E7A5E"/>
        <SolidColorBrush x:Key="ColorHover"  Color="#5F9774"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3A3F44"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#4A5568"/></Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="SaveButton" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource ColorOk}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{StaticResource ColorHover}"/></Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ComboBox" x:Key="DarkComboBoxJoined">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Background" Value="{StaticResource BgInput}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderMain}"/>
            <Setter Property="BorderThickness" Value="0,1,1,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                          Focusable="False" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="0,4,4,0">
                                            <ContentPresenter HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Path Fill="{StaticResource TextMuted}" Data="M0,0 L4,4 L8,0 Z" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="8,0,23,2" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource TextGreen}"/></Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="#1A202C" BorderThickness="1" BorderBrush="{StaticResource BorderLight}" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="150">
                                    <ScrollViewer Margin="0,2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Padding" Value="4,2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#4A5568"/></Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Canvas>
        <Border Name="borderMain" Canvas.Left="12" Canvas.Top="12" Width="390" Height="210"
                Background="{StaticResource BgPanel}" CornerRadius="8"
                BorderBrush="{StaticResource BorderMain}" BorderThickness="1">
            <Grid>
                <Canvas>
                    <TextBlock Text="OUTBOUND PROXY CONFIGURATION" Canvas.Left="15" Canvas.Top="12"
                               FontSize="10" Foreground="{StaticResource TextMuted}" FontWeight="Bold"/>
                    <Border BorderBrush="{StaticResource BorderLight}" BorderThickness="1" CornerRadius="4,0,0,4"
                            Canvas.Left="15" Canvas.Top="35" Width="120" Height="26">
                        <TextBlock Text="PROXY TYPE" FontSize="10" FontWeight="Bold"
                                   Foreground="{StaticResource TextMuted}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                    </Border>
                    <Rectangle Canvas.Left="134" Canvas.Top="35" Width="1" Height="26" Fill="{StaticResource BorderLight}"/>
                    <ComboBox Name="cmbProxyType" Canvas.Left="135" Canvas.Top="35" Width="120" Height="26"
                              FontSize="11" Style="{StaticResource DarkComboBoxJoined}">
                        <ComboBoxItem Content="SOCKS5"/><ComboBoxItem Content="HTTPS"/>
                    </ComboBox>
                    <TextBlock Text="Address/IP:" Canvas.Left="15" Canvas.Top="70" FontSize="11" Foreground="{StaticResource TextMuted}"/>
                    <TextBox Name="txtAddr" Canvas.Left="15" Canvas.Top="88" Width="240" Height="26"
                             Background="{StaticResource BgInput}" Foreground="{StaticResource TextGreen}"
                             BorderBrush="{StaticResource BorderMain}" BorderThickness="1" Padding="4"
                             FontSize="12" FontFamily="Consolas" VerticalContentAlignment="Center"/>
                    <TextBlock Text="Port:" Canvas.Left="270" Canvas.Top="70" FontSize="11" Foreground="{StaticResource TextMuted}"/>
                    <TextBox Name="txtPort" Canvas.Left="270" Canvas.Top="88" Width="95" Height="26"
                             Background="{StaticResource BgInput}" Foreground="{StaticResource TextGreen}"
                             BorderBrush="{StaticResource BorderMain}" BorderThickness="1" Padding="4"
                             FontSize="12" FontFamily="Consolas" VerticalContentAlignment="Center"/>
                    <Border BorderBrush="{StaticResource BorderLight}" BorderThickness="1" CornerRadius="4,0,0,4"
                            Canvas.Left="15" Canvas.Top="128" Width="120" Height="26">
                        <TextBlock Text="AUTHENTICATION" FontSize="10" FontWeight="Bold"
                                   Foreground="{StaticResource TextMuted}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                    </Border>
                    <Rectangle Canvas.Left="134" Canvas.Top="128" Width="1" Height="26" Fill="{StaticResource BorderLight}"/>
                    <ComboBox Name="cmbAuth" Canvas.Left="135" Canvas.Top="128" Width="120" Height="26"
                              FontSize="11" Style="{StaticResource DarkComboBoxJoined}">
                        <ComboBoxItem Content="Disabled"/><ComboBoxItem Content="Enabled"/>
                    </ComboBox>
                    <Canvas Name="panAuth" Canvas.Left="15" Canvas.Top="165" Visibility="Collapsed" Opacity="0">
                        <TextBlock Text="Username:" Canvas.Left="0" Canvas.Top="0" FontSize="11" Foreground="{StaticResource TextMuted}"/>
                        <TextBox Name="txtUser" Canvas.Left="0" Canvas.Top="18" Width="165" Height="26"
                                 Background="{StaticResource BgInput}" Foreground="{StaticResource TextGreen}"
                                 BorderBrush="{StaticResource BorderMain}" BorderThickness="1" Padding="4"
                                 FontSize="12" FontFamily="Consolas" VerticalContentAlignment="Center"/>
                        <TextBlock Text="Password:" Canvas.Left="185" Canvas.Top="0" FontSize="11" Foreground="{StaticResource TextMuted}"/>
                        <TextBox Name="txtPass" Canvas.Left="185" Canvas.Top="18" Width="165" Height="26"
                                 Background="{StaticResource BgInput}" Foreground="{StaticResource TextGreen}"
                                 BorderBrush="{StaticResource BorderMain}" BorderThickness="1" Padding="4"
                                 FontSize="12" FontFamily="Consolas" VerticalContentAlignment="Center"/>
                    </Canvas>
                    <Button Name="btnOk" Content="Save" Canvas.Left="175" Canvas.Top="168"
                            Width="90" Height="25" IsDefault="True" Style="{StaticResource SaveButton}"/>
                    <Button Name="btnCancel" Content="Cancel" Canvas.Left="275" Canvas.Top="168"
                            Width="90" Height="25" IsCancel="True"/>
                </Canvas>
                <Button Name="btnCloseDialog" HorizontalAlignment="Right" VerticalAlignment="Top"
                        Margin="0,6,6,0" Width="22" Height="22" Content="&#x2715;"
                        FontSize="10" FontWeight="Bold" Padding="0,-1,0,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#4A5568"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2D3748"/>
                                    <Setter Property="Foreground" Value="#E2E8F0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Canvas>
</Window>
